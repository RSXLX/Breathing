# 真实模型服务联调清单

状态：适配代码在项目中；本次打包未使用真实 API Key、未发起付费调用，也未下载运行真实模型权重。

## 共通前置

- [ ] 阅读原工程 `docs/PROVIDERS.md`、`docs/SECURITY.md`。
- [ ] 在服务端从 `.env.example` 复制配置；不要把实际密钥放到前端、Git 或公开 ZIP。
- [ ] 生成至少 24 字符随机 `ADMIN_TOKEN`，只开启一个服务进行首轮验证。
- [ ] 明确是否开启 `ALLOW_PAID_APIS`，首次将任务与令牌额度限制到小值。
- [ ] 核对服务商当前账号、模型标识、权限、授权、数据传输与真实计费。
- [ ] 固定经过测试的 SDK/模型版本，不能把已有默认值理解为以后永远可用。

## Tripo

一次真实文字任务 → 保存实际任务 ID → 验证重复提交的幂等性 → 等待真实结果 → 下载并保存源模型 → 在项目中导入 → 应用与录制 → 检查压缩/CORS/贴图 → 服务重启核对记录。

完成前，不把 `demo` 模式生成的样例当作真实 Tripo 成功。

## Decart

确认用户愿意传输画面 → 临时令牌字段与权限 → SDK 实际加载 → 输入流 → 真实返回首帧 → 提示词切换 → 客户端和服务商侧时限 → 隐藏页面/断网/手动停止 → 核对服务商用量。

测量首帧与端到端延迟；FPS 不代替延迟。应用内额度和断开计时器也不代替账单级限额。固定已验收的 SDK 版本再公开部署。

## World Labs

验证当前文字任务适配、真实 operation/world ID、错误与超时、返回地址可访问和授权。本包没有实现从手机照片自动重建世界，不用片尾预渲染效果冒充实时返回。

## 语义模型

主动加载 → 确认代码与权重下载 → 检验输入尺寸和标签映射 → 与规则模式分开显示 → 测白天/夜景/移动 → 模型失败不标成已识别 → 目标手机推理和内存验证。

## 记录模板

| 服务 | 模型/版本 | 日期/设备/网络 | 实际任务 ID | 输入授权 | 成功/失败证据 | 实际消耗 | 尚未通过项 |
|---|---|---|---|---|---|---|---|
| 待填 | 待填 | 待填 | 待填 | 待填 | 待填 | 待填 | 待填 |

不要把密钥写进记录表。费用以真实账单为准；本次没有给出新价格承诺。
