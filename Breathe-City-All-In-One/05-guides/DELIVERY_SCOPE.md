# 交付范围、未完成项与来源

## 这次新加入什么

新增总包导航、Mac/Windows 启动脚本、硬件 BOM、装配与接口设计草案、采购填写表、3D/结构件清单、创作提示词、实拍计划、产品摘要、部署/服务/验收指南，以及完整性校验记录。

原项目源码和资产按原 ZIP 保留；没有借打包修改业务实现、默认模型、付费开关或授权方式。旧预览、视频、截图和测试记录均有明确保存位置。

## 已有软件底座

前端画面输入、规则区域效果、长按、对比、录制导出、作品库、GLB 样例和模型查看；后端 SQLite 任务、幂等、演示 worker、真实服务适配器。具体边界以原项目 README 为准。

本次重新执行语法检查、43 项 Node 测试与 7 项本地集成检查；浏览器记录作为原交付证据保留，没有重新执行目标手机或付费服务验收。

## 没有包含的成品

真实模型权重、外部 SDK 的完整离线分发包、API Key/额度、真实生成模型或世界、现场实拍素材、正式三件城市生命体、生产部署账户/证书、实体电路/PCB、已验证固件、完整制造用 STL/STEP、实际装配硬件。

上述缺项不是“解压后自动下载”的隐藏步骤，也不应包装成已完成交付。硬件部分是路线与待办，采购之后还需要开发和实测。

## 能力命名

2D 区域合成 ≠ 空间 AR；预设蒙版 ≠ AI 识别；规则分类 ≠ 已加载真实语义模型；艺术化节奏 ≠ 人体心率；任务契约测试 ≠ 服务商端到端成功；历史截图 ≠ 本次真实运行录像。

## 来源与日期

代码/素材事实来自原 `README.md`、`docs/`、`public/assets/` 和实际代码。硬件与产品建议来自本轮对话，新增文字是归纳后的交接方案，不是硬件实测报告。

第三方接口文档与来源链接原样保留在 `01-project/breathe-city/docs/SOURCES.md`。原文标注的 2026-09-20 是原核对日期；本次 2026-09-21 为打包和本地复核日期，不代替实时外部调研。

## 校验与恢复

原始 ZIP 留在 `06-originals/`。源码逐文件 SHA-256 对照见 `07-verification/source-preservation.json`。总包逐文件摘要在 `SHA256SUMS.txt`。

如果以后自行修改源码，摘要不匹配是预期现象；不要据此删除自己的修改。归档原始包与工作目录分开保存。
